filter {
  if [type] == "java_app" {

    multiline {
      pattern => "^\d"
      what => "previous"
      negate => true
    }


    grok {
      match => ["message", "%{ET_JAVA_APP}"]
    }

    date {
      match => ["timestamp", "ISO8601"]
      remove_field => ["timestamp"]
    }
  }
}

filter {
  if [type] == "java_app" {

    multiline {
      pattern => "^\s"
      what => "previous"
    }


    grok {
      match => ["message", "%{ET_JAVA_APP}"]
    }

    date {
      match => ["timestamp", "ISO8601"]
      remove_field => ["timestamp"]
    }
  }
}
